<?xml version="1.0" encoding="utf-8"?>
<!-- Bitmap is padded in sync-android-brand-assets; light inset only for mask breathing room. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:bottom="14dp"
        android:left="14dp"
        android:right="14dp"
        android:top="14dp">
        <bitmap
            android:gravity="center"
            android:src="@drawable/ic_store_logo" />
    </item>
</layer-list>
