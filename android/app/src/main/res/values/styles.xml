<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Android 12+ shows a mandatory system splash. windowSplashScreenBackground must be a color.
      The animated "icon" uses ic_splash_system_neutral so the circular mask is same color as bg (invisible).
      Full-brand splash: splash_overlay.xml after super.onCreate.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@color/splash_brand_background</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="windowSplashScreenBackground">@color/splash_brand_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_system_neutral</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_brand_background</item>
        <item name="android:statusBarColor">@color/splash_brand_background</item>
        <item name="android:navigationBarColor">@color/splash_brand_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>